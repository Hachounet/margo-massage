---
import BaseLayout from "../layouts/BaseLayout.astro"
import CTA from "../components/CTA.astro";
import SwedishContent from "../components/SwedishContent.astro";
// Optimize our landing image and pass it as props to the BaseLayout (for preloading) and Landing (for rendering)
import { getOptimizedImage } from '../js/utils';
import landingImage from '../assets/images/pro-massage-table.jpg'; // <-- THE PATH TO THE ASSET YOU WANT TO PRELOAD - The asset must live in src

const optimizedImage = await getOptimizedImage(landingImage);
---


<BaseLayout
  title="Massage Suédois"
  description="Envie d’un massage Suédois à La Rochelle ? Je vous propose une séance à domicile pour relâcher les tensions et booster votre énergie."
  preloadedImage={optimizedImage}
>
  
   <SwedishContent h1Choice={true}/>
   <CTA/>
    
</BaseLayout>